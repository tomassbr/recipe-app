"use server";

/**
 * Sem přibudou server actions pro recepty (persist do DB), až nahradíš localStorage.
 * Vzor: `requireAdmin(await createClient())` před každou mutací, stejně jako v `admin.ts`.
 */

export {};
