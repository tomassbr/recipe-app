import { cn } from "@/utils/cn";

/** Shared glass surface — Design.md §3 + project tokens */
export const glassSurfaceClass = cn(
  "border border-white/50 bg-white/40 shadow-glass backdrop-blur-xl"
);
